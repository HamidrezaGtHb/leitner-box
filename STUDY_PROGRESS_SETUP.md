# راهنمای راه‌اندازی Progress Tracking

## مشکلات احتمالی و راه‌حل

### مشکل: "I Know It" کار نمی‌کند / Progress ثبت نمی‌شود

این به این معنی است که **migration هنوز در Supabase اجرا نشده است**.

## راه‌حل 1: استفاده از Supabase Dashboard (راحت‌تر)

1. برو به [Supabase Dashboard](https://supabase.com/dashboard)
2. پروژه خودت را انتخاب کن
3. از منوی سمت چپ، برو به **SQL Editor**
4. محتوای فایل `supabase/migrations/20260216000001_study_progress.sql` را کپی کن
5. در SQL Editor paste کن
6. دکمه **Run** را بزن

## راه‌حل 2: استفاده از Supabase CLI

```bash
# نصب Supabase CLI (اگر ندارید)
npm install -g supabase

# Login به Supabase
supabase login

# Link کردن پروژه (اولین بار)
supabase link --project-ref YOUR_PROJECT_REF

# اعمال migration
supabase db push
```

## بررسی موفقیت Migration

بعد از اجرای migration، می‌توانید در SQL Editor این query را اجرا کنید:

```sql
SELECT * FROM study_progress LIMIT 1;
```

اگر خطا ندید، یعنی موفق بوده! ✅

## تست Progress Tracking

1. **Refresh کنید** صفحه را (F5)
2. برید به بخش **Study**
3. یک category انتخاب کنید
4. یک کارت باز کنید
5. کارت را flip کنید (روی آن کلیک کنید)
6. دکمه **"I Know It"** را بزنید

### انتظار می‌رود:
- ✅ Badge از ○ به 🥉 تغییر کند
- ✅ بعد از 0.5 ثانیه، خودکار به کارت بعدی برود
- ✅ Stats در بالای صفحه به‌روز شود

### اگر هنوز کار نمی‌کند:

1. **Browser Console را باز کنید** (F12)
2. تب **Console** را ببینید
3. دوباره "I Know It" را بزنید
4. اگر خطایی دیدید، لطفاً به من بگویید

احتمالاً خطاهای زیر را خواهید دید:

#### خطا: `relation "study_progress" does not exist`
➡️ **راه‌حل**: Migration اجرا نشده - از راه‌حل 1 یا 2 بالا استفاده کنید

#### خطا: `new row violates row-level security policy`
➡️ **راه‌حل**: مطمئن شوید که login هستید

#### خطا: `Failed to save progress`
➡️ **راه‌حل**: 
1. مطمئن شوید login هستید
2. Console را چک کنید برای error دقیق‌تر

#### خطا: `duplicate key value violates unique constraint`
➡️ **راه‌حل**: این معمولاً مشکلی نیست - کد ما حالا این را handle می‌کند

## Debug: چک کردن Console

1. باز کنید: **F12** (Developer Tools)
2. تب **Console** را انتخاب کنید
3. دکمه "I Know It" را بزنید
4. **تمام پیغام‌های Error** را به من بگویید

پیغام‌های مفید:
- `Error fetching current progress:` - مشکل در خواندن progress
- `Error updating mastery:` - مشکل در ذخیره‌سازی
- `Error inserting mastery:` - مشکل در ایجاد record جدید

## پس از موفقیت

بعد از اینکه همه چیز کار کرد:
- هر user می‌تواند progress خودش را داشته باشد
- Progress به صورت real-time ذخیره می‌شود
- می‌توانید کارت‌ها را بر اساس mastery level فیلتر کنید
- Stats شما به‌روز می‌ماند

موفق باشید! 🎉
