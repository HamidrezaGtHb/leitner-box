# راهنمای ویژگی‌های پیشرفته

## 🆕 ویژگی‌های جدید اضافه شده

### 1. Auto-Generate (تولید خودکار کلمات)

**مسیر**: `/generate`

#### چگونه استفاده کنیم؟

1. برو به صفحه **Generate**
2. سطح CEFR را انتخاب کن (B1, B2, C1, C2)
3. تعداد کلمات را انتخاب کن (10, 25, 50, 100)
4. روی "Generate" کلیک کن
5. منتظر بمان تا AI لیست کلمات را تولید کند
6. پیش‌نمایش کلمات را ببین
7. گزینه بده:
   - **Enrich & Add to Active**: فوراً enrich شوند و به کارت‌های فعال اضافه شوند
   - **Add to Backlog**: به صف backlog اضافه شوند برای بعداً

#### چه کلماتی تولید می‌شود؟

AI براساس:
- سطح CEFR انتخابی
- کلمات پرکاربرد (high-frequency)
- ترکیبی از اسم، فعل، صفت
- کلمات کاربردی برای زندگی روزمره، کار، و تحصیل

#### محدودیت‌ها

- حداکثر 100 کلمه در هر بار
- هر کلمه نیاز به 1 API call دارد برای enrichment
- با rate limiting (5 requests/second)
- زمان تخمینی: ~20 ثانیه برای 50 کلمه

---

### 2. Backlog (صف کلمات آینده)

**مسیر**: `/backlog`

#### چیست؟

Backlog یک صف برای کلماتی است که می‌خواهی در آینده اضافه کنی، نه همین الان.

#### مزایا

- کلمات زیادی را جمع کن بدون فکر کردن به daily limit
- برنامه‌ریزی کن که کدام کلمات چه روزی اضافه شوند
- Priority تعیین کن (high/medium/low)
- Auto-add: کلمات ready خودکار اضافه می‌شوند

#### چگونه استفاده کنیم؟

**اضافه کردن به Backlog:**
1. از صفحه Generate، "Add to Backlog" را بزن
2. یا از OCR، بعد از extract کردن، "Add to Backlog" را بزن

**مدیریت Backlog:**
1. برو به صفحه **Backlog**
2. تب‌ها:
   - **Ready Now**: کلماتی که برای امروز schedule شده‌اند
   - **Scheduled**: کلمات آینده
   - **All**: همه کلمات backlog

**تنظیم Schedule:**
- روی دکمه‌های +1d, +3d, +7d کلیک کن
- کلمه برای آن روز schedule می‌شود

**تنظیم Priority:**
- High: اولویت بالا (اول اضافه می‌شود)
- Medium: اولویت متوسط
- Low: اولویت پایین

**اضافه کردن به Active:**
- روی "Add Now" کلیک کن
- یا "Add All Ready" برای اضافه کردن همه کلمات ready

#### Auto-Add از Backlog

اگر در Settings، "Auto-add from Backlog" فعال باشد:
- هر روز که app را باز کنی
- کلمات ready خودکار به active cards اضافه می‌شوند
- تا زمانی که daily limit پر نشود

---

### 3. OCR (استخراج از تصویر)

**مسیر**: `/ocr`

#### چیست؟

با OCR می‌توانی عکس از صفحه کتاب، لیست کلمات، یا هر متن آلمانی بگیری و کلمات را خودکار استخراج کنی.

#### چگونه کار می‌کند؟

**مراحل:**

1. **Upload Image**:
   - Drag & drop یا click to browse
   - عکس کتاب، لیست کلمات، یا متن آلمانی

2. **Extract**:
   - OCR متن را می‌خواند (Tesseract.js)
   - کلمات را tokenize می‌کند
   - کلمات آلمانی را filter می‌کند (با Umlaut، ß، و...)

3. **Select**:
   - لیست کلمات استخراج شده نمایش داده می‌شود
   - می‌توانی select/deselect کنی
   - Search برای پیدا کردن کلمات خاص

4. **Validate** (اختیاری):
   - AI کلمات را check می‌کند
   - کلمات نامعتبر حذف می‌شوند
   - فقط کلمات B1+ نگه داشته می‌شوند

5. **Enrich & Add**:
   - هر کلمه با AI enrich می‌شود
   - Progress bar نمایش داده می‌شود
   - اضافه به Active یا Backlog

#### نکات برای بهترین نتیجه

**عکس خوب:**
- روشن و واضح
- متن در focus باشد
- متن چاپی (نه دست‌نویس)
- بدون سایه یا تاریکی
- زاویه مستقیم (نه کج)

**عکس بد:**
- تار یا out of focus
- نور کم
- دست‌نویس
- کج یا چرخیده
- سایه روی متن

#### محدودیت‌ها

- فقط متن چاپی (printed text)
- زبان آلمانی (German language)
- دقت OCR: ~85-95% (بسته به کیفیت عکس)
- نیاز به validation دستی (select/deselect)

#### مثال Use Case

**Scenario**: یک صفحه از کتاب آلمانی داری با 30 کلمه جدید

1. عکس بگیر از صفحه
2. Upload کن در OCR
3. Extract: 28 کلمه پیدا می‌شود (2 تا اشتباه خوانده شده)
4. Select: کلمات اشتباه را deselect کن
5. Validate: AI چک می‌کند، 25 کلمه valid هستند
6. Enrich: هر 25 کلمه با meaning و examples enrich می‌شوند
7. Add to Backlog: برای 25 روز آینده schedule می‌شوند

نتیجه: در 2 دقیقه، 25 کلمه جدید آماده یادگیری!

---

## 🔧 تنظیمات پیشرفته

### در صفحه Settings:

(قرار است تب "Advanced" اضافه شود که شامل:)

**Backlog Settings:**
- ✅ Auto-add from Backlog: خودکار کلمات ready اضافه شوند
- 📊 Max Backlog Size: حداکثر تعداد (پیش‌فرض: 500)

**OCR Settings:**
- 🌍 OCR Language: German (پیش‌فرض)
- 📖 Min Level Filter: حداقل سطح برای validation (B1)

**Generate Settings:**
- 🎯 Default Level: سطح پیش‌فرض
- 📊 Default Count: تعداد پیش‌فرض

---

## 📊 Workflow پیشنهادی

### Workflow 1: Quick Daily Learning

```
هر روز:
1. Check "Due Today" → Review cards
2. Check "Backlog Ready" → Add 1-2 words from backlog
3. Done! (5-10 minutes)
```

### Workflow 2: Weekly Batch Learning

```
یکبار در هفته:
1. Generate 50 words at your level
2. Add all to Backlog
3. Schedule: 7 words/day for next week
4. Daily: Auto-add handles it for you
```

### Workflow 3: Textbook Study

```
هنگام مطالعه کتاب:
1. عکس از صفحه کلمات بگیر
2. OCR → Extract words
3. Select کلمات مورد نیاز
4. Add to Backlog
5. Over next days: Review naturally
```

---

## 🎯 ترکیب ویژگی‌ها

### مثال: آماده شدن برای B2 Exam

**هفته 1-2:**
- Generate 100 B2 words
- Add to Backlog
- Schedule: 10/day for 10 days

**هفته 3-4:**
- OCR از کتاب B2
- Extract ~100 words
- Add to Backlog
- Schedule: 7/day for 14 days

**هفته 5-8:**
- Auto-add از Backlog
- Daily review
- Track progress در Dashboard

**نتیجه:**
- 200 B2 words learned
- Systematic approach
- No overwhelm
- Steady progress

---

## ⚠️ نکات مهم

### API Usage

**Auto-Generate:**
- 50 words = 51 API calls (1 generate + 50 enrich)
- هزینه: ~$0.05 با OpenAI, رایگان با Gemini

**OCR:**
- 30 words extracted = 31 API calls (1 validate + 30 enrich)
- Client-side OCR (Tesseract) = رایگان

### Daily Limit

- Daily limit فقط کلمات **امروز created** را می‌شمارد
- کلمات از Backlog جزو limit نیستند تا زمانی که به active اضافه شوند
- کلمات failed از Box 2+ جزو limit نیستند

### Storage

- Backlog در localStorage ذخیره می‌شود
- حداکثر ~500 کلمه (قابل تنظیم)
- Backup توصیه می‌شود اگر backlog بزرگ داری

---

## 🚀 شروع سریع

### تست Auto-Generate:

```
1. Go to /generate
2. Select: B1, 10 words
3. Click Generate
4. Wait ~2 seconds
5. Click "Enrich & Add to Active"
6. Wait ~30 seconds
7. Done! 10 new B1 words in your collection
```

### تست Backlog:

```
1. Go to /generate
2. Generate 25 words
3. Add to Backlog (when implemented)
4. Go to /backlog
5. See all words scheduled
6. Click "Add All Ready"
```

### تست OCR:

```
1. Take photo of German text
2. Go to /ocr
3. Upload image
4. Click Extract
5. Select valid words
6. Add to Backlog or Active
```

---

## 📚 مستندات بیشتر

- برای مشکلات API: ببین `API_SETUP.md`
- برای Leitner system: ببین `LEITNER_SYSTEM.md`
- برای استفاده عمومی: ببین `USAGE_GUIDE.md`

---

موفق باشی! 🎉
