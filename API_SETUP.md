# راهنمای تنظیم API

## گام به گام برای Gemini API

### 1. دریافت API Key

1. برو به [Google AI Studio](https://ai.google.dev/)
2. روی "Get API Key" کلیک کن
3. اگر پروژه نداری، یکی بساز
4. روی "Create API Key" کلیک کن
5. کلید را کپی کن (شروع می‌شود با `AIza...`)

### 2. وارد کردن در اپلیکیشن

1. اپلیکیشن را باز کن: http://localhost:3000
2. برو به **Settings** (تنظیمات)
3. تب **API Key** را باز کن
4. روی دکمه **Gemini** کلیک کن
5. API Key را paste کن
6. روی **Save API Key** کلیک کن

✅ اگر کلید معتبر باشد، یک تیک سبز می‌بینی  
❌ اگر نامعتبر باشد، یک ضربدر قرمز می‌بینی

### 3. تست کردن

1. برو به صفحه **Home**
2. یک کلمه آلمانی بنویس (مثلاً: `Haus`)
3. Enter بزن یا روی + کلیک کن
4. اگر همه چیز درست باشد، ترجمه فارسی می‌بینی!

---

## رفع مشکلات رایج

### ❌ Error: "Invalid API key format"

**علت:** فرمت کلید اشتباه است

**راه حل:**
- کلید Gemini باید با `AIzaSy` شروع شود
- طول کلید باید حداقل 35 کاراکتر باشد
- مطمئن شو که کل کلید را کپی کرده‌ای (فاصله اضافه نباشد)

### ❌ Error: "API key is invalid"

**علت:** کلید در سرور Google معتبر نیست

**راه حل:**
1. مطمئن شو که کلید را از [AI Studio](https://ai.google.dev/) گرفتی
2. چند دقیقه صبر کن (گاهی فعال شدن کلید 1-2 دقیقه طول می‌کشد)
3. بررسی کن که API Key را delete نکرده باشی در Google AI Studio
4. اگر کلید قدیمی داری، یکی جدید بساز

### ❌ Error: "models/gemini-1.5-flash is not found"

**علت:** نام مدل اشتباه است یا در منطقه شما در دسترس نیست

**راه حل:**
- این مشکل برطرف شده است (اپلیکیشن از `gemini-pro` استفاده می‌کند)
- اگر هنوز مشکل دارید، آخرین نسخه کد را از GitHub بگیرید

### ❌ Error: "Gemini API error: 403"

**علت:** دسترسی به API محدود شده

**راه حل:**
1. برو به [Google AI Studio](https://ai.google.dev/)
2. کلید API خودت را پیدا کن
3. بررسی کن که "API restrictions" روی "None" باشد
4. اگر محدودیت داری، آن را حذف کن

### ❌ Error: "Gemini API error: 429"

**علت:** تعداد درخواست‌ها زیاد است

**راه حل:**
- سهمیه رایگان Gemini تمام شده
- چند دقیقه صبر کن و دوباره امتحان کن
- یا از OpenAI استفاده کن (اگر API key داری)

### ❌ Error: "Please enter your API key in Settings"

**علت:** API key ذخیره نشده

**راه حل:**
1. برو به **Settings**
2. تب **API Key** را باز کن
3. مطمئن شو که provider درست را انتخاب کردی (Gemini یا OpenAI)
4. API key را وارد کن
5. روی **Save API Key** کلیک کن
6. صفحه را refresh کن (F5)

---

## OpenAI API (جایگزین)

اگر Gemini کار نکرد، می‌توانی از OpenAI استفاده کنی:

### 1. دریافت API Key

1. برو به [OpenAI Platform](https://platform.openai.com/)
2. ثبت‌نام کن یا لاگین کن
3. برو به [API Keys](https://platform.openai.com/api-keys)
4. روی "Create new secret key" کلیک کن
5. کلید را کپی کن (شروع می‌شود با `sk-`)

### 2. وارد کردن در اپلیکیشن

1. برو به **Settings**
2. تب **API Key** را باز کن
3. روی دکمه **OpenAI** کلیک کن
4. API Key را paste کن
5. روی **Save API Key** کلیک کن

---

## مقایسه Gemini vs OpenAI

| ویژگی | Gemini | OpenAI |
|-------|--------|--------|
| قیمت رایگان | ✅ بیشتر | ⚠️ محدود ($5 credit) |
| سرعت | ⚡️ سریع‌تر | 🐢 کمی کندتر |
| دقت | ✅ عالی | ✅ عالی |
| محدودیت | 60 req/min | 3 req/min (free tier) |

**توصیه:** اول Gemini را امتحان کن (رایگان‌تر است)

---

## نکات مهم

### 🔐 امنیت

- API Key شخصی توست، با کسی به اشتراک نگذار
- اگر کلید leak شد، فوراً آن را delete کن و یکی جدید بساز
- در این اپلیکیشن، کلید فقط در مرورگر ذخیره می‌شود (LocalStorage)
- هیچ سرور خارجی کلید تو را ذخیره نمی‌کند

### 💾 LocalStorage

- کلید در مرورگر ذخیره می‌شود
- اگر cache مرورگر را پاک کنی، کلید پاک می‌شود
- باید دوباره وارد کنی

### 🌐 اینترنت

- برای استفاده از AI، اینترنت لازم است
- بدون اینترنت، نمی‌توانی کلمه جدید اضافه کنی
- اما می‌توانی کلمات قبلی را مرور کنی

---

## تست سریع

### برای Gemini:

```bash
curl -X POST \
  'https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=YOUR_API_KEY' \
  -H 'Content-Type: application/json' \
  -d '{"contents":[{"parts":[{"text":"Say hello"}]}]}'
```

اگر کار کرد، یک پاسخ JSON می‌بینی.

### برای OpenAI:

```bash
curl https://api.openai.com/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "gpt-4o-mini",
    "messages": [{"role": "user", "content": "Say hello"}]
  }'
```

---

## پشتیبانی

اگر مشکلت حل نشد:

1. Screenshot از ارور بگیر
2. کنسول مرورگر را باز کن (F12 → Console)
3. ارورهای قرمز را کپی کن
4. در GitHub Issues یک issue باز کن

لینک: https://github.com/HamidrezaGtHb/leitner-box/issues

---

موفق باشی! 🚀
